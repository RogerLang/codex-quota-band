{
  "name": "codex-quota-band",
  "version": "0.6.5",
  "private": true,
  "type": "module",
  "description": "Legacy Band 10 RPK retained while Xiaomi Smart Band 9 Pro adaptation is pending",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "prepare-assets": "node scripts/prepare-assets.js",
    "prebuild": "npm run prepare-assets",
    "build": "aiot build",
    "postbuild": "npm run test:built",
    "build:release": "npm run prepare-assets && aiot release && npm run test:built",
    "test:built": "node --test test/*.test.js"
  },
  "devDependencies": {
    "@aiot-toolkit/jsc": "1.0.3",
    "aiot-toolkit": "2.0.5",
    "sharp": "0.35.3"
  }
}
